# Iron Never Changes

Zero-build static site. Dev server: `.claude/launch.json` → port 4175.

Conventions, verify harnesses and the design contract live in the lab at
`/Users/dylanevans/Downloads/ezhd-lab` — the `ezhd-build` skill loads them.
Read `DESIGN.md` in this folder before changing markup or CSS.

Verify with `/ezhd-verify` before deploying. Never copy the harnesses in here.
