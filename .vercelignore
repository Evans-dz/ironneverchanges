# Anchored to the repo root — an unanchored "brand/" also matches
# assets/brand/ (gitignore semantics) and strips the site's own images,
# which is exactly what broke the first deployment.
/brand/
/README.md
/server.js
